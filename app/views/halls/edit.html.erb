<%= render partial: 'shared/error_messages', locals: { object: @hall } %>

<% content_for :title do %>
    <%= t('views.halls.edit_title', id: @hall.id) %>
<% end %>

<div class="card card-body my-4">
    <%= render partial: 'form' %>
</div>

<%= render partial: 'shared/error_messages', locals: { object: @order } %>

<% content_for :title do %>
    <%= t('views.orders.edit_title', id: @order.id) %>
<% end %>

<div class="card card-body my-4">
    <%= render partial: 'form' %>
</div>
